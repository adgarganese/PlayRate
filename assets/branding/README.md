# Branding

Logo assets have been removed. The app uses the **PlayRatePlaceholder** component for the PlayRate wordmark and tagline.

- Use `PlayRatePlaceholder` from `@/components/PlayRatePlaceholder` on auth and header areas.
