/**
 * Feature flags for enabling/disabling features
 */

/** Phone authentication - set true to enable phone login/signup (hidden for beta) */
export const FEATURE_PHONE_AUTH = false;

/** Soccer sport UI - set true to show soccer alongside basketball (beta is basketball-only) */
export const SOCCER_ENABLED = false;

/** Beta trim: hide court leaderboard section (advanced / unfinished for beta) */
export const BETA_HIDE_LEADERBOARD = true;
